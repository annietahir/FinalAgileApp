<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="agilegroup6.com.agilescanapp.MainActivity"
    tools:showIn="@layout/activity_main">


    <Button
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:text="@string/student_login"
        android:id="@+id/dualLoginButton"
        android:textSize="14sp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="199dp" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/loginDetails"
        android:layout_marginBottom="89dp"
        android:layout_above="@+id/dualLoginButton"
        android:layout_alignRight="@+id/dualLoginButton"
        android:layout_alignEnd="@+id/dualLoginButton"
        android:layout_alignLeft="@+id/dualLoginButton"
        android:layout_alignStart="@+id/dualLoginButton" />
    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/passwordDetails"
        android:layout_marginBottom="32dp"
        android:layout_above="@+id/dualLoginButton"
        android:layout_alignLeft="@+id/loginDetails"
        android:layout_alignStart="@+id/loginDetails"
        android:layout_alignRight="@+id/loginDetails"
        android:layout_alignEnd="@+id/loginDetails" />
</RelativeLayout>
